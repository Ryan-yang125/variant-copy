{
  "name": "variant-copy",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for copying HTML source from Variant community cards.",
  "scripts": {
    "check": "node --check content.js && node --check popup.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json', 'utf8')); console.log('manifest ok')\"",
    "icons": "python3 scripts/generate-icons.py",
    "store-assets": "python3 scripts/generate-store-assets.py",
    "package": "node scripts/package-extension.mjs"
  },
  "license": "MIT"
}
